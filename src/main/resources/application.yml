app:
  welcome-message: "Welcome to Spring blog!"
  admin-email: "admin@admin.com"

logging:
  pattern:
    dateformat: "yyyy-MM-dd HH:mm:ss"

spring:
  profiles:
    active: dev
  output:
    ansi:
      enabled: always
